import { Stack } from "@mui/material"

const HeroScene = () => {
    return (
        <Stack position={'absolute'} top={0} left={0} height={'100%'} width={'100%'}>
            
        </Stack>
    )
}

export default HeroScene