import { Box, Stack } from "@mui/material";
import theme from "@theme/theme";

const HeroSection = () => {
    return(
        <Box bgcolor={theme.palette.ne_lightblue.main} width={'100%'} height={'100vh'}>

        </Box>
    )
}

export default HeroSection;